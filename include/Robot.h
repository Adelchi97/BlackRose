//// Created by Elena Faillace on 26/06/2018.//#ifndef BLACKROSE_ROBOT_H#define BLACKROSE_ROBOT_H#include "Entity.h"#include "ResourceIdentifier.h"#include "ResourceHolder.h"class Robot : public Entity {public:    enum Type {        Red,        Gray,    };    //Takes as parameter its own type    Robot(Type type, const TextureHolder& textures);    virtual void drawCurrent(sf::RenderTarget &target, sf::RenderStates states) const final;private:    virtual void updateCurrent(sf::Time dt);    Type mType;    sf::Sprite mSprite;};#endif //BLACKROSE_ROBOT_H